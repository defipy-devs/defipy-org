---
title: "WithdrawSwap"
description: "Single-asset withdrawal (zap-out) for V2/V3. Removes liquidity and swaps the unwanted side back into the desired token in one shot."
---

Exit a V2/V3 LP position into a single token. `WithdrawSwap` is a **mutating** dispatch primitive that removes liquidity and *also* swaps the unwanted side back into the requested token, so the user ends up holding only `token_out`. **V2/V3 only** — see the architectural reason below.

:::note[When to use `WithdrawSwap` vs `RemoveLiquidity`]
[`RemoveLiquidity`](/api/primitive/core/remove-liquidity/) on V2/V3 returns *both* tokens at the pool's current ratio. `WithdrawSwap` does the burn and chains a final swap so you exit holding only the asset you specified — useful when one of the two tokens isn't what you actually want to hold. On Balancer and Stableswap, `RemoveLiquidity` already supports single-asset withdrawal first-class, so `WithdrawSwap` isn't needed.
:::

## Signature at a glance

| Protocol | Required call shape |
|---|---|
| Uniswap V2 | `WithdrawSwap().apply(lp, token_out, user_nm, amount_out)` |
| Uniswap V3 | `WithdrawSwap().apply(lp, token_out, user_nm, amount_out, lwr_tick, upr_tick)` |
| Balancer | ❌ Not supported — see "Why no Balancer/Stableswap section" below |
| Stableswap | ❌ Not supported |

## Common parameters

| Parameter | Type | Description |
|---|---|---|
| `lp` | `Exchange` | Initialized V2 or V3 pool with the user's LP shares to burn. |
| `token_out` | `ERC20` | The single token the user wants to receive. |
| `user_nm` | `str` | Account name whose LP shares are burned and who receives `token_out`. |
| `amount_out` | `float` | Total quantity of `token_out` the user expects to walk away with (combined burn + swap). |

## Protocol Variants

### Uniswap V2

The dispatcher computes the optimal split between "burn proceeds" and "swap proceeds" via `_calc_univ2_withdraw_portion` (closed-form using the pool reserves and the V2 `0.997` fee multiplier), then executes:

1. [`RemoveLiquidity`](/api/primitive/core/remove-liquidity/) for `p_out * amount_out` of `token_out` — returns both tokens.
2. [`Swap`](/api/primitive/core/swap/) the *other* token's proceeds back into `token_out`.
3. Combined output is `(swap result) + (RemoveLiquidity's token_out side)`.

| Parameter | Type | Notes |
|---|---|---|
| (no extras) | — | Common parameters only. |

```python
from defipy import WithdrawSwap

# Burn ETH/DAI LP shares; walk away with only DAI
withdrawn = WithdrawSwap().apply(lp, dai, "user", 1000)
# Returns the total DAI received (burn leg + swap leg combined)
```

### Uniswap V3

V3 uses the same two-step pattern, but the burn calculation goes through `SettlementLPToken` to compute the in-range L from the requested `amount_out` and the active sqrt-price.

| Parameter | Type | Notes |
|---|---|---|
| `lwr_tick` | `int` | Lower tick of the position to withdraw from. Must match the original mint. |
| `upr_tick` | `int` | Upper tick. |

```python
from defipy import WithdrawSwap

WithdrawSwap().apply(lp, eth, "user", 1, lwr_tick, upr_tick)
# Burns position liquidity inside [lwr_tick, upr_tick] and swaps the
# non-eth side back into ETH; returns total ETH received.
```

:::caution[Tick range matters]
The position you're exiting must already exist at `(lwr_tick, upr_tick)` — the dispatcher doesn't search across ranges. If you minted into multiple ranges, call `WithdrawSwap` once per range.
:::

### Why no Balancer/Stableswap section

`WithdrawSwap` exists because V2/V3's [`RemoveLiquidity`](/api/primitive/core/remove-liquidity/) returns *both* sides of the pair — getting back to a single asset requires an extra swap. **Balancer and Stableswap don't have this problem:** their `RemoveLiquidity` already supports specifying `token_out` and returning only that asset (`exit_swap_extern_amount_out` for Balancer, `lp.remove_liquidity(amount, token_out, user)` for Stableswap). The pool math handles the imbalance against the invariant directly.

Calling `WithdrawSwap().apply(lp, ...)` against a `BalancerExchange` or `StableswapExchange` will fail at the V2/V3 version check (the dispatcher is V2/V3-specific). Use [`RemoveLiquidity`](/api/primitive/core/remove-liquidity/) on those protocols.

## How `WithdrawSwap` interacts with the rest of the pipeline

1. [`Join`](/api/primitive/core/join/) → [`AddLiquidity`](/api/primitive/core/add-liquidity/) — pool initialized and LP positions held.
2. **Pre-exit analytics** — [`AnalyzePosition`](/agentic-primitives/position-analysis/) decomposes IL vs accumulated fees so the user can decide whether to exit; [`CalculateSlippage`](/agentic-primitives/execution/) projects the price impact of the internal swap leg.
3. **`WithdrawSwap`** — burn + swap to single asset (this primitive).

## See also

- [`RemoveLiquidity`](/api/primitive/core/remove-liquidity/) — the burn-only counterpart; also handles Balancer/Stableswap single-asset withdrawals
- [`SwapDeposit`](/api/primitive/core/swap-deposit/) — the inverse zap-in (V2/V3 only)
- [`AnalyzePosition`](/agentic-primitives/position-analysis/) — non-mutating IL/fee/PnL decomposition before exit
- [Tutorials → Uniswap V2 — Withdraw & Swap](/tutorials/uniswapv2/withdraw-swap/) — runnable walkthrough
- [The Primitive Contract](/primitive-contract/) — cross-cutting invariants
